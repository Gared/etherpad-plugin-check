name: Test with Etherpad 1.9.7
on:
  push:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  call:
    uses: ./.github/workflows/ep-plugin-test.yml
    with:
      etherpad-version: "1.9.7"
